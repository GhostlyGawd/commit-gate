{
  "name": "commit-gate-marketplace",
  "owner": { "name": "commit-gate" },
  "plugins": [
    {
      "name": "commit-gate",
      "description": "Enforce a conventional-commit template + issue refs on every commit via a commit-msg git hook, plus an advisory PreToolUse auto-stamp/nudge. Linking via your tracker's native integration.",
      "source": "./"
    }
  ]
}
